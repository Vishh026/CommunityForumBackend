const userAuth = (req,res,next) => {
    try{
        

        // token => cookies 
        // validate token present o not
        // validate token

        // get the user through token
        // find user
        // send user = req.user
    }catch(err){

    }
}